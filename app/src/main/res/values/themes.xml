<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CleanSwipe" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen Theme (AndroidX SplashScreen API) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo_light</item>
        <item name="postSplashScreenTheme">@style/Theme.CleanSwipe</item>
    </style>
</resources>